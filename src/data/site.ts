// TODO: update once the real domain is known.
// This project deploys to Cloudflare Workers (via @opennextjs/cloudflare),
// whose default URL is https://mahmoudghunaim.<your-workers-subdomain>.workers.dev
// — find <your-workers-subdomain> in the Cloudflare dashboard under
// Workers & Pages > (your account) > Workers subdomain, or use a custom domain.
export const SITE_URL = "https://mahmoudghunaim.pages.dev";

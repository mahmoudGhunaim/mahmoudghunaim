// TODO: update once the real domain/Cloudflare Pages URL is known.
// Cloudflare Pages defaults to https://<project-name>.pages.dev unless a
// custom domain is attached — update this the moment either is final.
export const SITE_URL = "https://mahmoudghunaim.pages.dev";
